# راهنمای استفاده سریع
# Quick Start Guide

## 📖 نحوه استفاده فرم

### مرحله 1️⃣: تنظیم اتصال GitHub

اگر می‌خواهید داده‌ها خودکار در GitHub ذخیره شوند:

1. از لینک زیر یک "Token" درست کنید:
   https://github.com/settings/tokens/new

2. دسترسی‌های مورد نیاز:
   - ☑️ repo (تمام دسترسی‌های repo)

3. Token را کپی کنید

4. در سایت:
   - میانبر: **Ctrl+G**
   - یا آیکون "تنظیم GitHub Token" کلیک کنید
   - Token را چسبانید
   - تأیید کنید

### مرحله 2️⃣: پر کردن فرم

#### اطلاعات الزامی (ستاره‌دار):
- **جنسیت**: مرد / زن
- **استان**: گیلان / مازندران / گلستان

#### سایر اطلاعات:
بقیه سوال‌ها را قسمت به قسمت پر کنید.

#### نکات درخصوص نوع سرطان:
- اگر جنسیت را "زن" انتخاب کردید → سرطان‌های زنانه نمایش داده می‌شود
- اگر جنسیت را "مرد" انتخاب کردید → سرطان‌های مردانه نمایش داده می‌شود

### مرحله 3️⃣: ذخیره رکورد

**خیار اول** (سریع):
```
Ctrl+Enter  ← میانبر صفحه کلید
```

**خیار دوم**:
```
دکمه "💾 ذخیره و بعدی"
```

✓ رکورد ذخیره می‌شود
✓ فرم خودکار پاک می‌شود
✓ آماده برای رکورد بعدی

### مرحله 4️⃣: دانلود داده‌ها

```
دکمه "⬇️ دانلود CSV"
```

تمام داده‌ها را می‌توان به صورت Excel (CSV) دانلود کرد.

## 🎯 نکات مهم

### آیکون‌های حالت:
- 📦 **حالت محلی** = بدون اتصال GitHub (داده‌ها محلی ذخیره می‌شود)
- ☁️ **متصل به GitHub** = Token معتبر + اتصال برقرار

### داده‌های خودکار (برای کودکان < 10 سال):
اگر سن تشخیص کمتر از 10 سال باشد:
- تعداد بارداری: 0
- تعداد زایمان: 0
- تعداد فرزند: 0
- سابقه قاعدگی: خیر
- روش پیشگیری: بدون

## ⌨️ میانبرهای صفحه کلید

| میانبر | عملکرد |
|--------|--------|
| **Ctrl+Enter** | ذخیره رکورد و بعدی |
| **Ctrl+R** | پاک کردن تمام فیلدها |
| **Ctrl+G** | باز کردن پنجره تنظیم GitHub |

## 🔒 امنیت

- Token شما **تنها در دستگاه شما** ذخیره می‌شود
- هیچ کسی نمی‌تواند Token شما را ببیند
- داده‌ها مستقیماً به GitHub ارسال می‌شود (بدون سرور میانی)

## 🆘 رفع مشکلات

### سوال: Token ثبت نمی‌شود
**پاسخ:**
1. دوباره تلاش کنید → Ctrl+G
2. اطمینان حاصل کنید Token صحیح است
3. مرورگر را refresh کنید (Ctrl+Shift+R)

### سوال: داده‌ها به GitHub نمی‌روند
**پاسخ:**
1. Token معتبر است؟ (ستاره بالا را بررسی کنید)
2. دسترسی "repo" فعال است؟
3. توانایی نوشتن در repo دارند؟

### سوال: من یک فایل CSV محلی می‌خواهم
**پاسخ:**
```
دکمه "دانلود CSV" بزنید
داده‌ها به عنوان فایل TEXT دانلود می‌شود
```

## 📚 سازمان داده‌ها

### اطلاعات ذخیره شده:
```
مستقر در: data/patients.csv
فرمت: CSV (علی‌الخصوص برای Excel)
رمزگذاری: UTF-8 (فارسی کامل)
```

### لینک GitHub:
```
https://github.com/medpromx/cancer
فایل: data/patients.csv
```

## 📞 پشتیبانی

اگر سوال دارید:
1. README اصلی را بخوانید
2. Console (F12) را برای خطاها بررسی کنید
3. GitHub Issues رو باز کنید

---

**نسخه**: 1.0.0
**تاریخ آخرین بروزرسانی**: 1403/11/16
